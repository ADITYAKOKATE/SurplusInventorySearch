{
  "name": "inventory-database-api",
  "version": "1.0.0",
  "description": "Inventory Database API — supplier and inventory CRUD with SQLite, grouped query sorted by total value.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js --ext js,sql",
    "db:seed": "node src/db/seed.js",
    "db:reset": "node src/db/reset.js",
    "db:setup": "npm run db:reset && npm run db:seed"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.7"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
